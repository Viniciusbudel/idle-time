# Agent Rules

## PowerShell Command Policy
- Do not use `Get-Content`.
- Do not use `Get-ChildItem`.
- Do not use `git-grep`.
- Use alternative PowerShell approaches instead (for example, .NET file APIs, `Select-String`, `git` commands, or other non-banned commands).
